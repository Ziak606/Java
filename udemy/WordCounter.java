package udemy;

public class WordCounter {
    //     * Create a program that counts the number of words in a given sentence.
    public static void main (String [] args){
        String str = "Create a program that counts";
        int length = str.length();

        System.out.println(length);
    }
}
